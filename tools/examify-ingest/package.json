{
  "name": "examify-ingest",
  "version": "0.1.0",
  "private": true,
  "description": "BankIR generate / validate / emit tooling for Examify. Generate writes IR only; emit still requires an explicit --apply.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "examify-ingest": "./bin/examify-ingest.mjs"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "examify-ingest": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.22.2 <23"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "tsx": "4.23.1",
    "typescript": "6.0.3"
  }
}
